// import { ComponentFixture, TestBed } from '@angular/core/testing';
// import { LoaderComponent } from './loader.component';

// // Describe el grupo de pruebas para LoaderComponent
// describe('LoaderComponent', () => {
//   let component: LoaderComponent;
//   let fixture: ComponentFixture<LoaderComponent>;

//   // Antes de cada prueba, configura el entorno de pruebas
//   beforeEach(async () => {
//     await TestBed.configureTestingModule({
//       declarations: [ LoaderComponent ] // Declara el componente a probar
//     })
//     .compileComponents(); // Compila los componentes declarados
//   });

//   // Antes de cada prueba, crea una instancia del componente y su fixture
//   beforeEach(() => {
//     fixture = TestBed.createComponent(LoaderComponent); // Crea el fixture
//     component = fixture.componentInstance; // Obtiene la instancia del componente
//     fixture.detectChanges(); // Detecta los cambios iniciales
//   });

//   // Prueba básica: verifica que el componente se crea correctamente
//   it('should create', () => {
//     expect(component).toBeTruthy(); // El componente debe existir
//   });

//   // Puedes agregar más pruebas aquí según la lógica del loader
// });
